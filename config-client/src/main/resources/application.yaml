server:
  port: 8081

spring:
  application:
    name: config-client

  profiles:
    active: prod

    cloud:
      config:
        uri: http://localhost:8888

  config:
    import: optional:configserver:http://localhost:8888