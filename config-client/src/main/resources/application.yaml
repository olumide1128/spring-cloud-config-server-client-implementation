server:
  port: 8081
  shutdown: graceful

spring:
  application:
    name: config-client

  profiles:
    active: test

    cloud:
      config:
        uri: http://localhost:8888

  config:
    import: optional:configserver:http://localhost:8888